# 인수인계 — 공공기관 채용공고 자동 수집

의뢰인이 직접 관리할 수 있게 쓴 문서다. **개발자 없이도 계속 돌아가야 한다**는 것이 목표다.

---

## 1. 지금 무엇이 돌고 있나

매일 아침 **07:00(한국시간)** 에 자동으로 실행된다.

1. 잡알리오에서 최근 공고 150건을 수집한다
2. 결과가 맞는지 10가지를 검사한다
3. **검사를 통과한 것만** `data/master.csv` 에 누적한다
4. 그날 새로 올라온 것만 `data/신규_최근.csv` 에 따로 담는다

손으로도 돌릴 수 있다 — GitHub 저장소 → **Actions** 탭 → `alio-collect` → **Run workflow**.

## 2. 결과는 어디서 보나

| 파일 | 내용 |
|---|---|
| `data/master.csv` | 지금까지 수집한 전체 공고 (중복 없음) |
| `data/신규_최근.csv` | **가장 최근 실행에서 새로 올라온 것만** |
| `품질검증리포트.md` | 그날 데이터가 믿을 만한지 검사한 결과 |

칼럼: 공고ID · 제목 · 기관명 · 근무지 · 고용형태 · 등록일 · 마감일 · 남은일수 · **마감임박** · 상태 · 상세URL

`마감임박` 이 `Y` 면 마감이 3일 이내다.

## 3. 구글 스프레드시트로 보기

시트 아무 칸에 아래 한 줄을 넣고 Enter 를 치면 끝이다. 이후로는 **자동으로 갱신된다.**

```
=IMPORTDATA("https://raw.githubusercontent.com/parkyejune-web/alio-recruit-watch/main/data/master.csv")
```

- 이 수식 입력은 **최초 1회만** 필요하다 (약 10초). 그 뒤로는 사람이 손댈 일이 없다.
- 신규분만 보려면 URL 끝을 `data/신규_최근.csv` 로 바꾼다.

> 왜 수식을 미리 넣어두지 않았나 — 스프레드시트 파일을 프로그램으로 만들면 수식이 글자로만 들어가고
> 계산되지 않는다. 사람이 한 번 Enter 를 쳐야 수식이 된다. 이건 구글 쪽 동작이라 우회할 방법이 없다.

## 4. 실패하면 어떻게 되나

- 저장소에 **이슈가 자동으로 생성**되고, GitHub 이 그 내용을 메일로 보낸다
- **실패한 날의 데이터는 저장되지 않는다.** 틀린 데이터가 쌓이는 것이 안 도는 것보다 나쁘기 때문이다
- 이슈에 실행 로그 링크가 들어 있다

실제로 겪은 실패: 3회 실행 중 1회, 대상 사이트 접속이 몇 분간 되지 않아 0건이 수집됐다.
재시도 3번을 하고도 넘기지 못한 구간이었다. **검사에서 걸려 누적되지 않았고 이슈가 생성됐다.**
다음 날 정상 실행되면 놓친 공고는 그때 다시 들어온다 (공고는 며칠간 목록에 남아 있다).

## 5. 사이트가 바뀌면

수집이 0건이 되거나 특정 칸이 비면 사이트 구조가 바뀐 것이다. 검사가 잡아내고 이슈가 생성된다.
고쳐야 할 곳은 `collect_alio.py` 한 파일이며, 파일 맨 위에 **이 사이트에서 실제로 밟았던 함정 3가지**가
주석으로 적혀 있다. 새로 맡는 사람은 그것부터 읽으면 된다.

## 6. 비용

- GitHub Actions : 공개 저장소는 **무료**
- 구글 스프레드시트 : 무료
- 합계 **월 0원**

## 7. 이 자료를 써도 되는 근거

잡알리오 저작권정책이 명시하고 있다.

- 저작권법 제24조의2(공공저작물의 자유이용) — 별도 이용허락 없이 자유이용 가능
- 공공데이터법 제1조·제3조 — 누구나 이용 가능하며 **영리 목적 이용을 포함한 자유로운 활용이 보장**

수집하지 않는 것: 로그인이 필요한 자료, 개인 식별 정보.
정보공개법 제9조 비공개대상정보와 제3자 권리가 포함된 정보는 애초에 대상이 아니다.
